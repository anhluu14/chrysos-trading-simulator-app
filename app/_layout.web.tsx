// The browser workspace uses local paper accounts and does not need native
// device APIs, wallet credentials, or a cloud account to start practicing.
export { default } from '@/components/paper/PaperWorkspace';
